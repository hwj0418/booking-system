const router = require('express').Router;
const Staff = require('../db/staff.db');
const bcrypt = require('bcryptjs');

const {loginValidation} = require('../validation');

router.post('/staff-login', async () =>{

    
});